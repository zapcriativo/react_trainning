import React from 'react'

// export default (props) -- outro metodo de exibir sao os atributos da funcao sem parenteses , serve apenas para componentes baseados em funcao 
export default props => (
	<h1>{props.value}</h1>
)